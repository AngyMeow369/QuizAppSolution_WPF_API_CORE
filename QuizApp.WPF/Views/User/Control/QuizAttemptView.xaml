<UserControl x:Class="QuizApp.WPF.Views.User.QuizAttemptView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:QuizApp.WPF.ViewModels.User"
             Height="600" Width="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="{Binding Quiz.Title}" FontSize="24" FontWeight="Bold"/>
            <TextBlock Text="Time Remaining:" FontSize="14" Margin="0,5,0,0"/>
            <TextBlock Text="{Binding RemainingSeconds}" FontSize="18" FontWeight="SemiBold" Foreground="Red" Margin="0,0,0,20"/>

            <ItemsControl ItemsSource="{Binding Quiz.Questions}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="{Binding Text}" FontSize="16" FontWeight="SemiBold"/>
                            <ItemsControl ItemsSource="{Binding Options}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <RadioButton Content="{Binding Text}"
                                                     GroupName="{Binding DataContext.Id, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                     IsChecked="{Binding Path=Id, Converter={StaticResource OptionIdToBoolConverter}, ConverterParameter={Binding DataContext.SelectedOptions}, Mode=TwoWay}"
                                                     />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Button Content="Submit Quiz" Command="{Binding SubmitCommand}" Background="Green" Foreground="White" Padding="10" Margin="0,20,0,0"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
