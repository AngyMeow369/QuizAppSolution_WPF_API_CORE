<UserControl x:Class="QuizApp.WPF.Views.User.UserResultsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="📈 My Results" FontSize="28" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>

        <!-- Loading Indicator -->
        <ProgressBar Grid.Row="1" Height="4" IsIndeterminate="True" 
                     Name="LoadingProgressBar"
                     Margin="0,0,0,20"/>

        <!-- Results List -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" 
                     Name="ResultsScrollViewer">
            <StackPanel>
                <ItemsControl ItemsSource="{Binding QuizResults}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding QuizTitle}" FontSize="16" FontWeight="SemiBold" Foreground="#2C3E50"/>
                                        <TextBlock Text="{Binding TakenAt, StringFormat='Taken on: MMM dd, yyyy - h:mm tt'}" 
                                               FontSize="12" Foreground="#7F8C8D" Margin="0,5,0,0"/>
                                    </StackPanel>

                                    <TextBlock Grid.Column="1" Text="{Binding Score, StringFormat={}{0}/{1}}" 
                                           FontSize="20" FontWeight="Bold" Foreground="#4CAF50" 
                                           VerticalAlignment="Center" Margin="0,0,20,0"/>

                                    <TextBlock Grid.Column="2" Text="{Binding Score, StringFormat={}{0}%}" 
                                           FontSize="16" FontWeight="SemiBold" 
                                           VerticalAlignment="Center"
                                           Name="ScoreTextBlock"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Empty State - Now properly inside the StackPanel -->
                <TextBlock Text="No quiz results yet" 
                       Name="EmptyResultsText"
                       HorizontalAlignment="Center" VerticalAlignment="Center" 
                       FontSize="16" Foreground="#BDC3C7" FontStyle="Italic"
                       Margin="0,20"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>