<Window x:Class="QuizApp.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuizApp.WPF"
        mc:Ignorable="d"
        Title="QuizApp - User Dashboard" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#F3F4F6">

    <Window.Resources>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#10B981"/>
            <!-- Modern Green -->
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#059669"/>
                    <!-- darker hover -->
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Navigation Button Style -->
        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Background" Value="#1F2937"/>
            <!-- Dark modern slate -->
            <Setter Property="Foreground" Value="#E5E7EB"/>
            <!-- Light gray text -->
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Left" 
                                              VerticalAlignment="Center"
                                              Margin="15,0,0,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#374151"/>
                    <!-- Hover Slate -->
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Card Style -->
        <Style x:Key="DashboardCard" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="14" Opacity="0.12" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="#111827">
            <!-- Modern Deep Navy -->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Logo/Header -->
                <Border Grid.Row="0" Background="#1F2937" Padding="20" CornerRadius="0,0,12,12">
                    <StackPanel>
                        <TextBlock Text="🎯 QuizApp" 
                                   Foreground="White" 
                                   FontSize="20" 
                                   FontWeight="Bold"/>
                        <TextBlock Text="User Dashboard" 
                                   Foreground="#9CA3AF" 
                                   FontSize="12" 
                                   Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Navigation -->
                <StackPanel Grid.Row="1" Margin="15,30,15,0">
                    <Button Content="📊 Dashboard" 
                            Style="{StaticResource NavButton}"
                            Command="{Binding NavigateToDashboardCommand}"/>

                    <Button Content="📝 My Quizzes" 
                            Style="{StaticResource NavButton}"
                            Command="{Binding NavigateToQuizzesCommand}"
                            Margin="0,10,0,0"/>

                    <Button Content="📈 Results" 
                            Style="{StaticResource NavButton}"
                            Command="{Binding NavigateToResultsCommand}"
                            Margin="0,10,0,0"/>

                    <Button Content="⚙️ Profile" 
                            Style="{StaticResource NavButton}"
                            Command="{Binding NavigateToProfileCommand}"
                            Margin="0,10,0,0"/>
                </StackPanel>

                <!-- User Info & Logout -->
                <Border Grid.Row="2" Background="#1F2937" Padding="15" CornerRadius="12,12,0,0">
                    <StackPanel>
                        <TextBlock Text="{Binding CurrentUsername}" 
                                   Foreground="White" 
                                   FontWeight="SemiBold"
                                   TextTrimming="CharacterEllipsis"/>
                        <TextBlock Text="{Binding UserRole}" 
                                   Foreground="#9CA3AF" 
                                   FontSize="11"
                                   Margin="0,2,0,10"/>

                        <Button Content="🚪 Logout" 
                                Command="{Binding LogoutCommand}"
                                Style="{StaticResource NavButton}"
                                Background="#EF4444" 
                                Foreground="White"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Column="1" Margin="20">
            <ContentControl Content="{Binding CurrentView}"/>
        </Grid>
    </Grid>
</Window>
