<UserControl x:Class="QuizApp.WPF.Views.Admin.ManageQuizzesView"  
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"  
          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"  
          Background="#F8FAFC"  
          FontFamily="Segoe UI">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header and Actions -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Manage Quizzes" FontSize="24" FontWeight="SemiBold" Foreground="#1F2937" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="➕ Add Quiz" Command="{Binding AddQuizCommand}" Margin="10,0"/>
                <Button Content="✏️ Edit" Command="{Binding EditQuizCommand}" Margin="10,0"/>
                <Button Content="🗑 Delete" Command="{Binding DeleteQuizCommand}" Margin="10,0"/>
                <Button Content="📋 Assign" Command="{Binding AssignQuizCommand}" Margin="10,0"/>
            </StackPanel>
        </StackPanel>

        <!-- Quizzes DataGrid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Quizzes}"  
              SelectedItem="{Binding SelectedQuiz, Mode=TwoWay}"  
              AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"  
              IsReadOnly="True" RowHeight="40">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category.Name}" Width="150"/>
               
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
    

</UserControl>  
