<Window x:Class="QuizApp.WPF.Views.Admin.QuizDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quiz" Height="500" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Quiz Title -->
        <TextBlock Text="Title:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,5,5"/>
        <TextBox Text="{Binding Quiz.Title, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" Margin="0,0,0,5"/>

        <!-- Category -->
        <TextBlock Text="Category:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,5,5"/>
        <ComboBox Grid.Row="1" Grid.Column="1" 
                  ItemsSource="{Binding Categories}" 
                  DisplayMemberPath="Name" 
                  SelectedValuePath="Id"
                  SelectedValue="{Binding Quiz.CategoryId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                  Margin="0,0,0,5"/>

        <!-- Start and End DateTime -->
        <TextBlock Text="Start Time:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,5,5"/>
        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,5">
            <DatePicker SelectedDate="{Binding Quiz.StartTime, Mode=TwoWay}" Width="120"/>
            <TextBox Text="{Binding Quiz.StartTime, StringFormat=HH:mm, Mode=TwoWay}" Width="60" Margin="5,0,0,0"/>
        </StackPanel>

        <TextBlock Text="End Time:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Top" Margin="0,0,5,5"/>
        <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal">
            <DatePicker SelectedDate="{Binding Quiz.EndTime, Mode=TwoWay}" Width="120"/>
            <TextBox Text="{Binding Quiz.EndTime, StringFormat=HH:mm, Mode=TwoWay}" Width="60" Margin="5,0,0,0"/>
        </StackPanel>

        <!-- Questions Selection -->
        <TextBlock Text="Questions:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Top" Margin="0,5,5,0"/>
        <ListBox Grid.Row="4" Grid.Column="1" ItemsSource="{Binding SelectedQuestions}" DisplayMemberPath="Text" SelectionMode="Extended" Height="150" Margin="0,5,0,0"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Save" Width="75" Margin="0,0,5,0" Command="{Binding SaveCommand}"/>
            <Button Content="Cancel" Width="75" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>
