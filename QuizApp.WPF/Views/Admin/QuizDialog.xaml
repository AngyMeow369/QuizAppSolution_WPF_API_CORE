<Window x:Class="QuizApp.WPF.Views.Admin.QuizDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:QuizApp.WPF.Converters"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        Title="Quiz"
        Height="750"
        Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#0F172A">

    <Window.Resources>
        <!-- Colors -->
        <Color x:Key="PrimaryColor">#6366F1</Color>
        <Color x:Key="DarkBackground">#0F172A</Color>
        <Color x:Key="CardBackground">#1E293B</Color>
        <Color x:Key="BorderColor">#334155</Color>
        <Color x:Key="HoverColor">#374151</Color>
        <Color x:Key="TextPrimary">#F1F5F9</Color>
        <Color x:Key="TextSecondary">#94A3B8</Color>
        <Color x:Key="DangerColor">#DC2626</Color>

        <!-- Brushes -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
        <SolidColorBrush x:Key="DarkBackgroundBrush" Color="{StaticResource DarkBackground}"/>
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="{StaticResource CardBackground}"/>
        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>
        <SolidColorBrush x:Key="HoverBrush" Color="{StaticResource HoverColor}"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimary}"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondary}"/>
        <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource DangerColor}"/>

        <!-- Converters -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:InverseBooleanConverter x:Key="InverseBooleanConverter"/>

        <!-- TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D3748"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="CaretBrush" Value="{StaticResource TextPrimaryBrush}"/>
        </Style>

        <!-- ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#2D3748"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- ComboBox Item Style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#2D3748"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <!-- ListBox Style -->
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        </Style>

        <!-- ListBoxItem Style -->
        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}"
                                BorderThickness="1"
                                BorderBrush="{StaticResource BorderBrush}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource HoverBrush}"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Button Style -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4F46E5"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        
        

        <!-- Danger Button Style -->
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Quiz Title -->
            <TextBlock Text="Title:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
            <TextBox Text="{Binding Quiz.Title, UpdateSourceTrigger=PropertyChanged}"
                     Grid.Row="0" Grid.Column="1"
                     Margin="0,5,0,15"
                     Padding="6"
                     FontSize="14"/>

            <!-- Category -->
            <TextBlock Text="Category:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
            <ComboBox Grid.Row="1" Grid.Column="1"
                      ItemsSource="{Binding Categories}"
                      DisplayMemberPath="Name"
                      SelectedValuePath="Id"
                      SelectedValue="{Binding Quiz.CategoryId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      Margin="0,5,0,15"
                      Padding="6"
                      FontSize="14"/>

            <!-- Start Time -->
            <TextBlock Text="Start Time:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Top" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
            <xctk:DateTimePicker Grid.Row="2" Grid.Column="1"
                                 Value="{Binding Quiz.StartTime, Mode=TwoWay}"
                                 Format="Custom"
                                 FormatString="dd/MM/yyyy HH:mm"
                                 Width="220"
                                 Margin="0,5,0,15"
                                 Padding="5"
                                 Background="#2D3748"
                                 Foreground="{StaticResource TextPrimaryBrush}"
                                 BorderBrush="{StaticResource BorderBrush}"/>

            <!-- End Time -->
            <TextBlock Text="End Time:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Top" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
            <xctk:DateTimePicker Grid.Row="3" Grid.Column="1"
                                 Value="{Binding Quiz.EndTime, Mode=TwoWay}"
                                 Format="Custom"
                                 FormatString="dd/MM/yyyy HH:mm"
                                 Width="220"
                                 Margin="0,5,0,15"
                                 Padding="5"
                                 Background="#2D3748"
                                 Foreground="{StaticResource TextPrimaryBrush}"
                                 BorderBrush="{StaticResource BorderBrush}"/>

            <!-- Questions -->
            <TextBlock Text="Questions:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Top" FontWeight="SemiBold" Margin="0,0,5,0" Foreground="{StaticResource TextPrimaryBrush}"/>
            <StackPanel Grid.Row="4" Grid.Column="1" Margin="0,5,0,15">
                <ListBox ItemsSource="{Binding SelectedQuestions}" SelectedItem="{Binding SelectedQuestion, Mode=TwoWay}" Height="300">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="1" BorderBrush="{StaticResource BorderBrush}" CornerRadius="6" Padding="10" Margin="0,4" Background="{StaticResource CardBackgroundBrush}">
                                <StackPanel>
                                    <!-- Question Text -->
                                    <TextBox Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                             FontWeight="SemiBold"
                                             FontSize="14"
                                             Margin="0,0,0,8"
                                             ToolTip="Enter your question here"/>

                                    <!-- Options -->
                                    <ItemsControl ItemsSource="{Binding Options}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" Margin="0,3">
                                                    <CheckBox IsChecked="{Binding IsCorrect, Mode=TwoWay}" VerticalAlignment="Center" Foreground="{StaticResource TextPrimaryBrush}"/>
                                                    <TextBox Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                             Width="220" Margin="8,0,0,0"
                                                             ToolTip="Enter option text"/>
                                                    <Button Content="X" 
                                                            Command="{Binding DataContext.DeleteOptionCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                            CommandParameter="{Binding}"
                                                            Margin="8,0,0,0" Width="25" Height="25"
                                                            Background="#DC2626"
                                                            Foreground="White"
                                                            FontWeight="Bold"
                                                            FontSize="10"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                    <!-- Add Option (pass QuestionViewModel as CommandParameter) -->
                                    <Button Content="Add Option"
                                            Command="{Binding DataContext.AddOptionCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}"  
                                            Margin="0,8,0,0"
                                            Width="100"
                                            HorizontalAlignment="Left"
                                            Style="{StaticResource SecondaryButton}"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <!-- Add/Delete Question Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="Add Question" Command="{Binding AddQuestionCommand}" Width="110" Margin="0,0,8,0"/>
                    <Button Content="Delete Question" Command="{Binding DeleteQuestionCommand}" Width="130" Style="{StaticResource SecondaryButton}"/>
                </StackPanel>

            </StackPanel>

            <!-- Save / Cancel -->
            <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button Width="90" Margin="0,0,10,0" Command="{Binding SaveCommand}" Padding="6"
                        IsEnabled="{Binding IsSaving, Converter={StaticResource InverseBooleanConverter}}">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="Save" Margin="0,0,5,0"/>
                        <Grid Width="16" Height="16" Visibility="{Binding IsSaving, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Ellipse Width="16" Height="16" Stroke="White" StrokeThickness="2" Opacity="0.5"/>
                            <Path Data="M8,0 A8,8 0 1 1 7.999,0 Z" Fill="White">
                                <Path.RenderTransform>
                                    <RotateTransform x:Name="Spinner" Angle="0" CenterX="8" CenterY="8"/>
                                </Path.RenderTransform>
                                <Path.Triggers>
                                    <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                        <BeginStoryboard>
                                            <Storyboard RepeatBehavior="Forever">
                                                <DoubleAnimation Storyboard.TargetName="Spinner" Storyboard.TargetProperty="Angle" From="0" To="360" Duration="0:0:1"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Path.Triggers>
                            </Path>
                        </Grid>
                    </StackPanel>
                </Button>
                <Button Content="Cancel" Width="90" Command="{Binding CancelCommand}" Padding="6" Style="{StaticResource SecondaryButton}"/>
            </StackPanel>

        </Grid>
    </ScrollViewer>
</Window>