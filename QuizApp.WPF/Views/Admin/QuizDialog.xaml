<Window x:Class="QuizApp.WPF.Views.Admin.QuizDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        Title="Quiz"
        Height="750"
        Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F6FA">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Quiz Title -->
            <TextBlock Text="Title:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <TextBox Text="{Binding Quiz.Title, UpdateSourceTrigger=PropertyChanged}"
                     Grid.Row="0" Grid.Column="1"
                     Margin="0,5,0,15"
                     Padding="6"
                     FontSize="14"/>

            <!-- Category -->
            <TextBlock Text="Category:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <ComboBox Grid.Row="1" Grid.Column="1"
                      ItemsSource="{Binding Categories}"
                      DisplayMemberPath="Name"
                      SelectedValuePath="Id"
                      SelectedValue="{Binding Quiz.CategoryId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      Margin="0,5,0,15"
                      Padding="6"
                      FontSize="14"/>

            <!-- Start Date & Time -->
            <TextBlock Text="Start Time:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Top" FontWeight="SemiBold"/>
            <xctk:DateTimePicker Grid.Row="2" Grid.Column="1"
                                 Value="{Binding Quiz.StartTime, Mode=TwoWay}"
                                 Format="Custom"
                                 FormatString="dd/MM/yyyy HH:mm"
                                 Width="220"
                                 Margin="0,5,0,15"
                                 Padding="5"/>

            <!-- End Date & Time -->
            <TextBlock Text="End Time:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Top" FontWeight="SemiBold"/>
            <xctk:DateTimePicker Grid.Row="3" Grid.Column="1"
                                 Value="{Binding Quiz.EndTime, Mode=TwoWay}"
                                 Format="Custom"
                                 FormatString="dd/MM/yyyy HH:mm"
                                 Width="220"
                                 Margin="0,5,0,15"
                                 Padding="5"/>

            <!-- Questions Section -->
            <TextBlock Text="Questions:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Top" FontWeight="SemiBold" Margin="0,0,5,0"/>

            <StackPanel Grid.Row="4" Grid.Column="1" Margin="0,5,0,15">

                <!-- Editable Questions List -->
                <ListBox ItemsSource="{Binding SelectedQuestions}" SelectedItem="{Binding SelectedQuestion, Mode=TwoWay}" Height="300">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="1" BorderBrush="#DDD" CornerRadius="5" Padding="6" Margin="0,4">
                                <StackPanel>
                                    <!-- Editable Question Text -->
                                    <TextBox Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" 
                                             FontWeight="SemiBold"
                                             FontSize="14"
                                             Margin="0,0,0,5"
                                             ToolTip="Enter your question here"/>

                                    <!-- Editable Options -->
                                    <ItemsControl ItemsSource="{Binding Options}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" Margin="0,3">
                                                    <CheckBox IsChecked="{Binding IsCorrect}" VerticalAlignment="Center"/>
                                                    <TextBox Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                             Width="220" Margin="5,0,0,0"
                                                             ToolTip="Enter option text"/>
                                                    <Button Content="🗑" 
                                                            Command="{Binding DataContext.DeleteOptionCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                                                            CommandParameter="{Binding}"
                                                            Margin="5,0,0,0" Width="25" Height="25"
                                                            Background="#F8D7DA" BorderBrush="#F5C2C7"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                    <!-- Add Option Button -->
                                    <Button Content="Add Option"
                                            Command="{Binding DataContext.AddOptionCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                                            CommandParameter="{Binding}"
                                            Margin="0,5,0,0"
                                            Width="100"
                                            HorizontalAlignment="Left"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <!-- Question Controls -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="Add Question" Command="{Binding AddQuestionCommand}" Width="110" Margin="0,0,5,0"/>
                    <Button Content="Delete Question" Command="{Binding DeleteQuestionCommand}" Width="130"/>
                </StackPanel>

                <!-- Preview Section -->
                <GroupBox Header="Preview" Margin="0,15,0,0">
                    <ScrollViewer Height="200" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding SelectedQuestions}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="0,5,0,5">
                                        <!-- Question Text -->
                                        <TextBlock Text="{Binding Text}" FontWeight="Bold" FontSize="14"/>

                                        <!-- Options -->
                                        <ItemsControl ItemsSource="{Binding Options}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal" Margin="10,2,0,2">
                                                        <CheckBox IsChecked="{Binding IsCorrect}" IsEnabled="False"/>
                                                        <TextBlock Text="{Binding Text}" Margin="5,0,0,0"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </GroupBox>

            </StackPanel>

            <!-- Save / Cancel Buttons -->
            <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button Content="Save" Width="90" Margin="0,0,10,0" Command="{Binding SaveCommand}" Padding="6"
                        Background="#FF2196F3" Foreground="White" BorderBrush="#FF1976D2" BorderThickness="1"/>
                <Button Content="Cancel" Width="90" Command="{Binding CancelCommand}" Padding="6"
                        Background="#FFE0E0E0" Foreground="Black" BorderBrush="#FFBDBDBD" BorderThickness="1"/>
            </StackPanel>

        </Grid>
    </ScrollViewer>
</Window>
