<UserControl x:Class="QuizApp.WPF.Views.Admin.QuizDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             Background="#F5F6FA">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Quiz Details" FontSize="24" FontWeight="Bold" Margin="10"/>

        <Grid Grid.Row="1" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <!-- Quiz List -->
            <Border Grid.Column="0" 
                    Background="White" 
                    CornerRadius="8" 
                    Padding="10" 
                    Margin="0,0,10,0" 
                    BorderBrush="#DDD" 
                    BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Quizzes" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,10"/>
                    <ListBox ItemsSource="{Binding Quizzes}" 
                             SelectedItem="{Binding SelectedQuiz}" 
                             DisplayMemberPath="Title"
                             Height="400"/>
                </StackPanel>
            </Border>

            <!-- Questions & Options -->
            <Border Grid.Column="1"
                    Background="White"
                    CornerRadius="8"
                    Padding="10"
                    BorderBrush="#DDD"
                    BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Questions" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,10"/>
                    <ScrollViewer Height="400" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding SelectedQuiz.Questions}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="5" Padding="8" Margin="0,0,0,8">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Text}" FontWeight="Bold" FontSize="14"/>
                                            <ItemsControl ItemsSource="{Binding Options}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Horizontal" Margin="10,2,0,2">
                                                            <CheckBox IsChecked="{Binding IsCorrect}" IsEnabled="False"/>
                                                            <TextBlock Text="{Binding Text}" Margin="5,0,0,0"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
